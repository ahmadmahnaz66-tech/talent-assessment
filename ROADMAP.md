# نقشه راه و برنامه‌های توسعه آتی سامانه آپادانا (ROADMAP)

---

## ۱. ماژولار کردن فرانت‌اند (`admin.js`)
هدف: شکستن فایل سنگین `admin.js` به ماژول‌های سبک، مستقل و ایمن در مسیر `js/admin/`:
- [ ] `auth.js`: مدیریت ورود، خروج، تغییر پسورد و نشست پرسنل
- [ ] `students.js`: ثبت تکی، فیلتر پایه/کلاس، ریست رمز، ایمپورت/اکسپورت اکسل دانش‌آموزان
- [ ] `roadmaps.js`: صدور کارنامه هوش مصنوعی، چارت‌های گاردنر و ذخیره مستقیم PDF
- [ ] `exposure.js`: ثبت و ویرایش ارزیابی مجاورت‌سازی ۲ هفته‌ای مربیان
- [ ] `skills.js`: مدیریت مهارت‌ها، بانک سوالات و اکسل گویه‌ها
- [ ] `ai-test.js`: پنل اختصاصی تست پینگ، سلامت کلیدها و مصرف توکن Gemini (مخصوص مدیر ارشد)
- [ ] بازنویسی `admin.js` به عنوان هاب مرکزی لود ماژول‌ها و تغییر تب‌ها

---

## ۲. ربات تلگرام Serverless (Cloudflare Webhook)
هدف: مدیریت سریع خدمات پرتکرار بدون بار روی سرور و هاست مجزا:
- [ ] ساخت ربات در BotFather و تعریف سکرت `TELEGRAM_BOT_TOKEN` در کلادفلر
- [ ] پیاده‌سازی اندپوینت `functions/api/telegram-webhook.js`
- [ ] **بخش اولیا:** بازیابی و ریست خودکار رمز عبور پورتال با دکمه Send Contact بر اساس تطابق با `parent_phone` در دیتابیس D1
- [ ] **بخش اولیا:** ارسال اعلان و لینک مستقیم مشاهده کارنامه جدید پس از صدور توسط هوش مصنوعی
- [ ] **بخش پرسنل:** ورود دائمی با دستور `/login`، دریافت اعلان ثبت‌نام‌های جدید و پایش سلامت کلیدهای هوش مصنوعی

---

## ۳. ارتقای امنیت و کنترل دسترسی (Security Hardening)
هدف: بستن آسیب‌پذیری‌ها و ایمن‌سازی اندپوینت‌های حساس:
- [ ] **بررسی و ایمن‌سازی `upload-to-github.js`:** اطمینان از عدم دسترسی عمومی و حذف هرگونه توکن هاردکدشده گیت‌هاب
- [ ] **محدودسازی نرخ درخواست (Rate Limit):** ایجاد سد در برابر اسپم روی اندپوینت‌های هوش مصنوعی (`explain-question` و `generate-roadmap`) جهت حفظ سهمیه توکن
- [ ] **احراز هویت سشن در صدور کارنامه:** اعتبارسنجی کد ملی درخواستی با نشست فعال کاربر جهت جلوگیری از دسترسی اولیا به کارنامه سایر دانش‌آموزان
- [ ] **سخت‌گیرانه‌تر کردن احراز هویت ادمین:** بازگشت به اعتبارسنجی امضای توکن JWT به جای اتکا به فیلدهای متنی بدنه درخواست

---

## ۴. بهبودهای فرانت‌اند و تجربه کاربری (UX)
- [ ] بهینه‌سازی فرم نظرسنجی اولیا (`parent-survey.html`)
- [ ] کش کردن پاسخ‌های عمومی هوش مصنوعی (مانند توضیحات پرتکرار گویه‌ها) جهت کاهش مصرف توکن
