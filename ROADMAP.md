# نقشه راه و برنامه‌های توسعه آتی سامانه آپادانا (ROADMAP)

---

## ۱. ماژولار کردن فرانت‌اند (`admin.js`)
هدف: شکستن فایل سنگین `admin.js` به ماژول‌های سبک، مستقل و ایمن در مسیر `js/admin/`[cite: 4]:
- [ ] `auth.js`: مدیریت ورود، خروج، تغییر پسورد و نشست پرسنل[cite: 4]
- [ ] `students.js`: ثبت تکی، فیلتر پایه/کلاس، ریست رمز، ایمپورت/اکسپورت اکسل دانش‌آموزان[cite: 4]
- [ ] `roadmaps.js`: صدور کارنامه هوش مصنوعی، چارت‌های گاردنر و ذخیره مستقیم PDF[cite: 4]
- [ ] `exposure.js`: ثبت و ویرایش ارزیابی مجاورت‌سازی ۲ هفته‌ای مربیان[cite: 4]
- [ ] `skills.js`: مدیریت مهارت‌ها، بانک سوالات و اکسل گویه‌ها[cite: 4]
- [ ] `ai-test.js`: پنل اختصاصی تست پینگ، سلامت کلیدها و مصرف توکن Gemini (مخصوص مدیر ارشد)[cite: 4]
- [ ] بازنویسی `admin.js` به عنوان هاب مرکزی لود ماژول‌ها و تغییر تب‌ها[cite: 4]

---

## ۲. بهینه‌سازی سرعت و پردازش هوش مصنوعی (AI Performance & Streaming)
هدف: کاهش زمان انتظار صدور کارنامه و جلوگیری از معطلی کاربر:
- [ ] **پردازش موازی کارنامه (Parallel Multi-Key Generation):** شکستن پرامپت کارنامه تحلیلی به ۲ بخش مجزا (بخش اول: تحلیل هالند و استعدادها | بخش دوم: توصیه‌ها و نقشه ۳ ماهه) و فراخوانی هم‌زمان با دو کلید API مختلف از طریق `Promise.all` جهت کاهش ۵۰ درصدی زمان صدور.
- [ ] **پیاده‌سازی پاسخ‌دهی زنده (Streaming SSE):** فعال‌سازی متد استریمینگ در مودال کارنامه تحلیلی تا اولیا و مشاوران متن را خط به خط و لحظه‌ای مشاهده کنند.
- [ ] **کش کردن پرامپت‌های استاتیک و گویه‌ها:** ذخیره پاسخ‌های پرتکرار در D1 یا KV جهت جلوگیری از درخواست‌های تکراری به Gemini.

---

## ۳. ربات تلگرام Serverless (Cloudflare Webhook)
هدف: مدیریت سریع خدمات پرتکرار بدون بار روی سرور و هاست مجزا:
- [ ] ساخت ربات در BotFather و تعریف سکرت `TELEGRAM_BOT_TOKEN` در کلادفلر
- [ ] پیاده‌سازی اندپوینت `functions/api/telegram-webhook.js`
- [ ] **بخش اولیا:** بازیابی و ریست خودکار رمز عبور پورتال با دکمه Send Contact بر اساس تطابق با `parent_phone` در دیتابیس D1[cite: 1]
- [ ] **بخش اولیا:** ارسال اعلان و لینک مستقیم مشاهده کارنامه جدید پس از صدور توسط هوش مصنوعی
- [ ] **بخش پرسنل:** ورود دائمی با دستور `/login`، دریافت اعلان ثبت‌نام‌های جدید و پایش سلامت کلیدهای هوش مصنوعی

---

## ۴. ارتقای امنیت و کنترل دسترسی (Security Hardening)
هدف: بستن آسیب‌پذیری‌ها و ایمن‌سازی اندپوینت‌های حساس:
- [ ] **بررسی و ایمن‌سازی `upload-to-github.js`:** اطمینان از عدم دسترسی عمومی و حذف هرگونه توکن هاردکدشده گیت‌هاب[cite: 1]
- [ ] **محدودسازی نرخ درخواست (Rate Limit):** ایجاد سد در برابر اسپم روی اندپوینت‌های هوش مصنوعی (`explain-question` و `generate-roadmap`) جهت حفظ سهمیه توکن[cite: 1, 2]
- [ ] **احراز هویت سشن در صدور کارنامه:** اعتبارسنجی کد ملی درخواستی با نشست فعال کاربر جهت جلوگیری از دسترسی اولیا به کارنامه سایر دانش‌آموزان[cite: 2]
- [ ] **سخت‌گیرانه‌تر کردن احراز هویت ادمین:** بازگشت به اعتبارسنجی امضای توکن JWT به جای اتکا به فیلدهای متنی بدنه درخواست

---

## ۵. بهبودهای فرانت‌اند و تجربه کاربری (UX)
- [ ] بهینه‌سازی فرم نظرسنجی اولیا (`parent-survey.html`)
- [ ] نمایش پیشرفت زنده درصد همگام‌سازی‌های دسته‌ای در پنل مدیریت[cite: 4]
